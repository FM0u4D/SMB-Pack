# SMB-Pack
Reproducible SOC case study demonstrating secure SMB over WireGuard, where access failures emerge from layered controls (VPN, firewall, Windows policy, Samba auth, Linux permissions) with deterministic validation via scripts and CI-ready checks.
